{
  "name": "streaming-markov-chain-builder",
  "version": "0.0.0-development",
  "description": "A Markov chain builder that accepts input text as a stream and outputs a stream of n-grams.",
  "main": "src/index",
  "repository": "https://github.com/flotwig/node-streaming-markov-chain-builder",
  "author": "Zach Bloomquist <zach@bloomqu.ist>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "yarn build && `npm bin`/jest src/*.spec.js",
    "build": "tsc",
    "release": "yarn test && npx semantic-release"
  },
  "files": [
    "src/*.js",
    "src/*.ts",
    "!*.spec.ts",
    "!*.spec.js"
  ],
  "devDependencies": {
    "@types/chai": "^4.2.4",
    "@types/jest": "^24.0.22",
    "@types/node": "^12.12.7",
    "chai": "^4.2.0",
    "jest": "^24.9.0",
    "snap-shot-it": "^7.9.0",
    "typescript": "^3.7.2"
  },
  "release": {
    "repositoryUrl": "https://github.com/flotwig/node-streaming-markov-chain-builder.git"
  }
}
